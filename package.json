{
  "private": true,
  "workspaces": [
    "documentation",
    "nestjs",
    "cli"
  ],
  "scripts": {
    "clean": "rimraf **/node_modules **/dist node_modules",
    "build": "yarn workspace @norest/nestjs build && yarn workspace @norest/cli build",
    "watch:cli": "yarn workspace @norest/cli build -w",
    "watch:nestjs": "yarn workspace @norest/nestjs build -w",
    "watch:documentation": "?",
    "start:cli": "yarn workspace @norest/cli start:dev",
    "start:nestjs": "yarn workspace @norest/nestjs start:dev",
    "start:documentation": "?",
    "db:mongo": "docker start mongo && docker run -it --rm -p 8081:8081 --link mongo:mongo mongo-express"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.0",
    "eslint-plugin-import": "^2.20.1",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2"
  }
}
